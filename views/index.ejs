<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/../styles/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <title>Taxable Muni Deals</title>
</head>
<body>

    <div class="container my-3">
        <div class="p-4 text-center bg-body-tertiary rounded-3">
          <h1 class="text-body-emphasis">Taxable Muni Transaction Tracker</h1>
          <p class="col-lg-8 mx-auto fs-5 text-muted">
            The industry source for taxable muni deals
          </p>
          <div class="d-inline-flex gap-2">
            <button class="d-inline-flex align-items-center btn btn-primary btn-lg px-4 " type="button">
              View Deals
            </button>
            <button class="btn btn-outline-secondary btn-lg px-4 " type="button">
              Submit Deal
            </button>
          </div>
        </div>
      </div>
      
      
      <% if (locals.editSubmission) { %>
        <ul style="list-style-type: none;">
            <li>
                <%- include("partials/editDealForm.ejs", { editSubmission: editSubmission }) %>
            </li>
        </ul>
      <% } else if (locals.submissions) { %>
        <ul style="list-style-type: none;">
            <% submissions.forEach(submission => { %>
            <li>
                <%- include("partials/displayDeals.ejs", { submission: submission }) %>
            </li>
            <% }); %>
        </ul>
        <%- include("partials/submitDealForm.ejs") %>
      <% } else { %>
        <p> editSubmission and Submission not found</p>
        <%- include("partials/submitDealForm.ejs") %>
      <% } %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    
    <!-- include script to format and restrict input to forms         -->
    <%- include("partials/formatInputs.ejs") %> 

</body>
</html>